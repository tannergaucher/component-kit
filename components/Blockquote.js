import styled from 'styled-components'
import { Blockquote as BaseBlockquote } from 'rebass'

const Blockquote = styled(BaseBlockquote)``

Blockquote.defaultProps = {
  fontSize: 3
}

export default Blockquote
