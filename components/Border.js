import styled from 'styled-components'
import { Border as BaseBorder } from 'rebass'

const Border = styled(BaseBorder)``

Border.defaultProps = {
  border: 1
  // borderColor: 'gray'
}

export default Border
